package com.fourguard.wms.domain.enums;

/**
 * Operational status of a Forklift Operator in the WMS catalog.
 * This is a catalog-level status (not a system user status).
 */
public enum ForkliftOperatorStatus {
    ACTIVO,
    INACTIVO
}
